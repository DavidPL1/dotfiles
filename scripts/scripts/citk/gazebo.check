#!/bin/bash

#cd ~/citk/dist && git pull --rebase

$HOME/citk/jenkins/job-configurator --on-error=continue \
--cache-directory=/tmp/cache \
platform-requirements \
$HOME/citk/dist/distributions/gazebo-8-nightly.distribution \
-p=ubuntu
